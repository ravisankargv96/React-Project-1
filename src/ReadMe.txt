Following Logic is there in navigating UI

HomePage -> endPoint("/")
  Goto Products -> endPoint("/products")
  Goto NotFound -> endPoint("/not-found")

Products:
  react component is <ProductList/>

NotFound:
  react component is <NotFound/>

Note:
  Be Cautious about the packages, other versions 
  (older/Newer) are causing issues.
  i.e. Things didn't go as we expected.


Completed the entire project, 
Write the documentation in detail
for future revision
